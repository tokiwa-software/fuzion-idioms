c := for
       t := "#", t + v.hex 2
       i in 1..5 : 2
       a1 := (c1.substring i i+2).parseI32Hex.val
       a2 := (c2.substring i i+2).parseI32Hex.val
       v := (a1 + a2) / 2
     else
       t
