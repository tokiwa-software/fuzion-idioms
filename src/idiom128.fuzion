BreadthFirstTraverse(f node -> unit, root node) =>
  queue := [root].asList
  BreadthFirstTraverse f queue
  BreadthFirstTraverse(f node -> unit, queue List<node>) =>
    if queue.count > 0 then
      t := queue.first
      f t
      BreadthFirstTraverse f (queue.drop 1 ++ t.children)