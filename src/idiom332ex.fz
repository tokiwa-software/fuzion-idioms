ex322 is

  # Create a map with integers as keys (1-4)

  m := container.map_of [
    (1, "P=NP(?)"),
    (2, "The Hodge Conjecture"),
    (3, "The Poincare Conjecture"),
    (4, "The Riemann Hypothesis")
  ]

  # Create keys to a list
  keys := m.keys

  # Go through each key in a loop
  for k in keys do

    say "Key: {k}"
