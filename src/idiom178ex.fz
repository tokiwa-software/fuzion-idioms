ex178 is

  for i := 0.3, i+0.3
  while i < 1.3 do

    x := i
    y := i*2

    x1 := 0.0
    x2 := 1.0
    y1 := 0.0
    y2 := 2.0

    b := x1 < x < x2 && y1 < y < y2

    say "Point $x,$y is {if b then "" else "not "}inside of rectangle $x1,$y1,$x2,$y2"
