ex218 is

  intersection(T type: equatable, s1, s2 container.Set T) =>  # NYI: intersection missing from stdlib
    s1.as_list.filter (x -> s2.contains x)

  a := ["eins", "zwei", "drei"]
  b := ["drei", "vier", "fünf"]
  c := intersection (container.set_of_ordered a) (container.set_of_ordered b)
  say c
