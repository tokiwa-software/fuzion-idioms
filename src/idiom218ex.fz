ex218 is

  intersection(T type: hasEquals T, s1, s2 Set T) =>  # NYI: intersection missing from stdlib
    s1.as_list.filter (x -> s2.contains x)

  a := ["eins", "zwei", "drei"]
  b := ["drei", "vier", "fünf"]
  c := intersection (set_of_ordered a) (set_of_ordered b)
  say c
