ex35a is

  A(val i32) is
  B(val bool) is
  C(val String) is

  f A -> B := a -> B (a.or_panic > 3)
  g B -> C := b -> C (if b.or_panic then "T" else "F")

  fog := g ∘ f

  say (fog (A 10)).or_panic
  say (fog (A 2)).or_panic
  say (fog (A 0)).or_panic
