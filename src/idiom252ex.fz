ex252 is

  for counter in 1..4 do
    condition => counter %% 3
    x := condition ? "a" : "b"
    say x

  for counter in 5..8 do
    condition => counter %% 3
    x := if condition then "a" else "b"
    say x
