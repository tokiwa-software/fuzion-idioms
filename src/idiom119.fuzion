# preserving order
#
x := for
       r list String := nil, if e ∈ s then r
                                      else cons e r
       s := set_of_ordered (Sequence String).type.empty, s.add e
       e in x
     else
       r.reverse

# not preserving order
#
x := set_of_ordered x
