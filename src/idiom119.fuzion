# preserving order
x := for
       r list<string> := nil, if (s.has e) r else cons e, r
       s := psSet<string>, s.add e
       e in x
     else
       r.reverse

# not preserving order
x := for
       s := psSet<string>, s.add e
       e in x
     else
       s.asArray
