ex69 is

  # instate random effect with seed 42
  #
  # random numbers can be generated within the effect environment
  # i.e. within the lambda `()->...`
  #
  Random.simple 42 ()->

    for _ in 0..5 do
      random_number := Random.env.next_u32 100
      say random_number
