m := container.ordered_map String String .of [ "Hund", "Katze", "Maus" ] [ "dog", "cat", "mouse" ]

k := "Katze"
if m.has k
  say "$k was found"
else
  say "$k not found"

w := "Wellensittich"
if m.has w
  say "$w was found"
else
  say "$w not found"
