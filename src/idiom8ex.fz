ex8 is

  x := container.ps_map String i64 .of [("one", 1), ("two", 2)]

/* NYI: with better syntactic sugar, this should become

  x := [ "one" => 1,
         "two" => 2 ]

*/

  for s in ["one", "two", "three"] do
    say "$s maps to $(x[s])"
