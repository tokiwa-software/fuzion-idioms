ex245 is

  T is
    a := 42
    b := "Hello"

  x := T

  # NYI: The output is currently dependent on the backend's implementation
  # of intrinsic Object.asStrig.  The output coulde be more useful.
  say x
