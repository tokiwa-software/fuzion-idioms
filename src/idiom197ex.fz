ex197 is

  path := path.of "path/to/file"

  lines := io.file.read path .bind (.split "\n")

  match lines
    l Sequence String => say l   # logic in case of success
    err error         => say err # error handling logic
