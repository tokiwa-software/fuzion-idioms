ex263 is
  log2 (n f64) f64 =>
    n.log / (f64 2).log

  log2d (n f64) f64 =>
    (log2 n).floor

  log2u (n f64) f64 =>
    (log2 n).ceil

  (1..12).map (.as_f64)
         .for_each f->
           say "$f $(log2d f) $(log2u f)"
