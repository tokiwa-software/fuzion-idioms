ex196 is

  n i64 := 20
  m i64 := 13

  foo (E type : mutate, a E.array i64, i i64) ! E =>
    a[i] := 42

  mi : mutate is
  mi.instate_self ()->
    a := (mi.array i64).new n _->0
    a.indices.for_each (i -> a[i] := i)

    say a

    ((i64 1)..m : 2).for_each (foo a)

    say a
