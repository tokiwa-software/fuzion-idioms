ex196 is

  mi : mutate is
  mi.go ()->

    foo (a Mutable_Array i32, i i32) unit is
      a[i] := 42

    a := (mutate.array i32).type.new mi 10 0
    a.indices | (i -> a[i] := i)

    say a

    a.indices & (i -> !(i%%2)) | (i -> foo a i)

    say a
