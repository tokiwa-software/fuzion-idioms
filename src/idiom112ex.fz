ex112 is

  map := (orderedMap
    [ "un", "deux", "trois", "quatre", "cinq" ]
    [ "ichi", "ni", "san"  , "yon"   , "go"   ] )

  for k in sorted_array_of map.keys do
    say "$k => {map[k]}"

  # since map is already orderedMap, map.keys are already ordered
  for k in map.keys do
    say "$k => {map[k]}"
